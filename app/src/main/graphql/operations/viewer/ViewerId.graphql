query ViewerId {
    Viewer {
        id
        options {
            profileColor
            staffNameLanguage
            titleLanguage
        }
        mediaListOptions {
            scoreFormat
        }
    }
}
