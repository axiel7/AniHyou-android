query ChildComments($threadCommentId: Int) {
    ThreadComment(id: $threadCommentId) {
        id
        comment
        likeCount
        createdAt
        user {
            id
            name
            avatar {
                medium
            }
        }
        childComments
    }
}