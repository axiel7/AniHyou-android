query MediaSorted($page: Int, $perPage: Int, $type: MediaType, $sort: [MediaSort], $country: CountryCode, $isAdult: Boolean) {
    Page(page: $page, perPage: $perPage) {
        media(type: $type, sort: $sort, countryOfOrigin: $country, isAdult: $isAdult) {
            ...ExploreMedia
        }
        pageInfo {
            ...CommonPage
        }
    }
}
