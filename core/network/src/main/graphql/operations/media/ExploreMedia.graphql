fragment ExploreMedia on Media {
    ...BasicMediaDetails
    coverImage {
        large
    }
    averageScore
    nextAiringEpisode {
        episode
        airingAt
        timeUntilAiring
    }
    genres
    mediaListEntry {
        ...BasicMediaListEntry
    }
}