fragment ExploreMedia on Media {
    ...BasicMediaDetails
    coverImage {
        large
    }
    meanScore
    nextAiringEpisode {
        episode
        airingAt
        timeUntilAiring
    }
    genres
    mediaListEntry {
        ...BasicMediaListEntry
    }
}